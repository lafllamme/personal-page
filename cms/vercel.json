{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "pnpm run build",
  "installCommand": "pnpm install --frozen-lockfile",
  "functions": {
    "src/app/**/*.{js,ts}": {
      "maxDuration": 30
    }
  },
  "env": {
    "NEXT_SHARP": "0"
  }
} 