                    # =============================================
                    # Personal Page Monorepo - Environment Setup
                    # =============================================
                    #
                    # This monorepo uses WORKSPACE-SPECIFIC environment files:
                    #
                    # 📁 cms/.env         - Contains all CMS (Payload) configuration
                    # 🎨 frontend/.env     - Contains all Frontend (Nuxt) configuration
                    #
                    # No root .env file is needed! Each workspace is self-contained.
                    #
                    # To set up:
                    # 1. Copy cms/.env.example to cms/.env and fill in your values
                    # 2. Copy frontend/.env.example to frontend/.env and fill in your values
                    #
                    # Benefits:
                    # ✅ Clean separation of concerns
                    # ✅ No environment variable duplication
                    # ✅ Self-contained workspaces
                    # ✅ Easier deployment and development